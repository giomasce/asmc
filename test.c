
int glob2;
int glob;

main(argc, argv) {
  int a;
  a = glob + argc + 1;
  int b;
  b = argv || a;
  int c;
  c = 0; //"{hello}[there](world)";
  b = c + a * a;
  return b;
  int d;
  d = 1;
  return d;
}

test(x) {
  int y;
  y = x;
  return x;
}

sum(x, y) {
  int z;
  z = x + y;
  return z;
}

hello(bye) {
  bye = 10 + 20 + 30;
}

empty() {
  //return hello(100);
}
